<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/disc"
        android:label="@string/app_name"
        android:roundIcon="@drawable/disc"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.IniciarActivity"></activity>
        <activity
            android:name=".activities.NovaEquipeActivity"
            android:label="@string/title_activity_nova_equipe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.NovaPessoaActivity"
            android:label="@string/title_activity_nova_pessoa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.EquipeActivity"
            android:label="@string/title_activity_equipe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.PessoaActivity"
            android:label="@string/title_activity_pessoa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.AmbienteActivity"
            android:label="@string/title_activity_ambiente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.NovoAmbienteActivity"
            android:label="@string/title_activity_novo_ambiente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>